#include <iostream>
using namespace std;

int main()
{
	cout << "Object - Objects have stats and behaviors. \n Example: A Dog has states- color, name , breed as well as behaviors - wagging, barking, eating\n an object is an instance of a class";

	cout << "A class can be defined as a template/blueprint that describrs the behaviors/states that objet of its type support.";

	cout << "Methods - A Methods is basically a behavior. A class can contain many methods. it is in methods where the logics are written, data is manipulated and all the actions are executed.";

	cout << "Instance Variables - Each object has its unique set of instance variables. An object's state is created by the values assigned to these instance variables.";


	/*
	The C++ lagnauge difneds several headers, which contian information that it either necessary or usuful to your program.
	For this program, the header <iostream> is needed.

	The line using namespace std; tells the compier to use the std namespaced. 
	Namespaces are a relatively recent addtion to C++

	The next line // main() is where program execution begins

	The line int main() is the main function where program execution begins.

	The next line return 0; terminates main()function and cause it to return value - to the claling process.


	*/

	//cout << "\n??=";

	return 0;
}